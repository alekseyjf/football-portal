# Dependencies
node_modules
.pnp
.pnp.js
.pnp.*

# Package managers (keep lockfile committed)
# pnpm-lock.yaml — НЕ ігнорувати
# Проєкт лише на pnpm — lock-файли інших менеджерів не комітимо
package-lock.json
yarn.lock

# Turborepo
.turbo

# Builds & caches
dist
.next
out
build
*.tsbuildinfo

# Tests
coverage

# Environment — не комітити секрети
.env
.env.local
.env.*.local
!.env.example

# Cookie jar-и curl (містять JWT / refresh-токени)
cookies.txt
*.cookies

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE (прибери рядок, якщо хочеш комітити спільні .vscode)
.idea

# Deploy
.vercel

# TLS / keys
*.pem

# Local DB (якщо з’явиться SQLite тощо)
*.db
*.db-journal

# Кастомний вивід Prisma (якщо увімкнеш output у схемі)
**/generated/prisma
